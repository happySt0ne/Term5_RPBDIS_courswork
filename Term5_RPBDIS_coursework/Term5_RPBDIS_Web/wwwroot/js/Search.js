window.onload = function () {
    // Считывание значения из скрытого элемента
    var tableValue = document.getElementById("tableValue").value;
    document.getElementById("choosingList").value = tableValue;
}